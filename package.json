{
  "name": "ingestor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run tauri -- dev",
    "build": "npm --prefix frontend run tauri -- build",
    "frontend:dev": "npm --prefix frontend run dev -- --host 127.0.0.1 --port 1420 --strictPort",
    "frontend:build": "npm --prefix frontend run build",
    "backend:serve": "backend\\.venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8765"
  }
}
